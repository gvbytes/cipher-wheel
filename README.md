# cipher-wheel
simple caesar cipher script. can encrypt/decrypt and also brute force the shift key if you don't know it.
just wrote it to mess around with basic crypto concepts.

run it with: `python cipher.py`
